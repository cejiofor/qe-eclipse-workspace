DROP TABLE IF EXISTS STUDENTS CASCADE;
CREATE TABLE STUDENTS (
	STUDENT_ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1) PRIMARY KEY,
	NAME VARCHAR(45),
	EMAIL VARCHAR(45)
);

